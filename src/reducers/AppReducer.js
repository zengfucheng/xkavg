/**
 *
 * name: AppReducer
 * date: 2018/12/22
 * author: cengfucheng
 * about: 主程序reducer
 *
 */

export default function (state, action) {
    let { type, payload} = action;

    switch (type) {

        default : return state;
    }
}
